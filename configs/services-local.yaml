# Service configuration for local development (no Docker)
# Each service runs on localhost with different ports

version: "1.0"
mode: local

services:
  feature-registry:
    grpc:
      host: localhost
      port: 50051
    rest:
      host: localhost
      port: 8080
    mcp:
      host: localhost
      port: 9000

  ecosystem-core:
    grpc:
      host: localhost
      port: 50052
    rest:
      host: localhost
      port: 8081
    mcp:
      host: localhost
      port: 9001

  claude-arena:
    grpc:
      host: localhost
      port: 50053
    rest:
      host: localhost
      port: 8082
    mcp:
      host: localhost
      port: 9002

  sdlc-workflow-manager:
    grpc:
      host: localhost
      port: 50054
    rest:
      host: localhost
      port: 8083
    mcp:
      host: localhost
      port: 9003

  test-manager:
    grpc:
      host: localhost
      port: 50055
    rest:
      host: localhost
      port: 8084
    mcp:
      host: localhost
      port: 9004

  async-runner:
    grpc:
      host: localhost
      port: 50056
    rest:
      host: localhost
      port: 8085
    mcp:
      host: localhost
      port: 9005

infrastructure:
  redis:
    host: localhost
    port: 6379
